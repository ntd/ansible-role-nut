# {{ ansible_managed }}

{% for ups in nut_ups %}
[{{ ups.name }}]
    driver = {{ ups.driver }}
    port   = {{ ups.device }}
    desc   = "{{ ups.description }}"
    {% extra|indent(4) %}
{% endfor %}

{% nut_ups_extra %}
